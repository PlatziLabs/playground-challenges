export class Nodo {
  constructor(valor) {
    this.valor = valor;
    this.siguiente = null;
  }
}

export function isPalindrome(cabeza) {
  // Tu código aquí 👇
  return true;
}

function clon(obj) {
  return JSON.parse(JSON.stringify(obj));
}
