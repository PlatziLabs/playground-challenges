export class Course {
  constructor({
    name,
    classes = [],
  }) {
    this.name = name;
    this.classes = classes;
  }

  get name() {
    // Tu código aquí 👈
  }

  set name(nuevoNombrecito) {
    // Tu código aquí 👈
  }
}